{
  "name": "docker-learning-api",
  "version": "1.0.0",
  "description": "Beginner Node.js API for learning Docker. v1 uses static data; v2 will add a database.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
